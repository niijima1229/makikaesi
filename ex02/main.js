// 数字を与えると、0以上の数であればplusの文字を、0以下であればminusの文字を出力する関数を書きなさい
// if文を使って実装すること
// 関数のプロトタイプ function putPlusOrMinus(num);

const num1 = 10;
const num2 = -90;
const num3 = 0;

function putPlusOrMinus(num) {
  // ここに処理を書いてね
}

putPlusOrMinus(num1);
putPlusOrMinus(num2);
putPlusOrMinus(num3);
